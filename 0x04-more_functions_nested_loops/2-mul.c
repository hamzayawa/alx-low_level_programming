#include "main.h"

/**
  * mul - Multiplies two integers
  * @a: First number to multiply
  * @b: Second number to multiply with first number
  *
  * Return: The result of multiplication between a and b param
  */
int mul(int a, int b)
{
	return (a * b);
}
