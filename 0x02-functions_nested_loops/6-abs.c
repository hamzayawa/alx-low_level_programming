#include "main.h"
/**
 * _abs - Returns the absolute value of an integer.
 * @n: The input integer.
 *
 * Return: The absolute value of the integer.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
